# 隐私说明

## 数据处理范围

- 扩展只在用户主动打开并点击扩展的 Bilibili、YouTube、小鹅通视频页或 linux.do 主题页中读取对应内容。
- 视频字幕与 linux.do 所选楼层仅用于生成当前设备上的 Markdown、剪贴板内容、下载文件或写入本机 Obsidian。
- linux.do 适配器使用当前浏览器会话访问主题 JSON 和缺失楼层，不读取或导出 Cookie，也不绕过主题权限。
- 扩展设置保存在 Chrome 的本地扩展存储中，包括目标目录、本机 Local REST API 地址和 API Key。
- API Key 只会作为 Bearer 认证头发送到 `127.0.0.1` 或 `localhost`；扩展会拒绝连接非本机 API 地址。

## 不会做的事情

- 不读取或导出 Cookie、密码、支付信息、浏览历史或其他网站数据。
- 不上传付费课程、字幕正文、论坛帖子或 Obsidian 内容到第三方服务。
- 不调用其他字幕插件的私有后端、令牌或额度。
- 不下载完整视频，不绕过登录、付费、访问控制或 DRM。
- 不在当前设备上自动安装或运行语音识别模型。
- 不把 Local REST API Key 写入 Markdown、日志、源码、发布包或视频平台请求。

## 可分发包

发布包不包含个人 Vault ID、Local REST API Key、个人保存目录、账号、联系方式、绝对文件路径、浏览记录、真实课程内容或真实帖子内容。API Key 和保存目录由每位用户首次使用时自行填写，并只保存在其 Chrome 本地扩展存储中。
